class Pizza

  def initialize(size, type, crust)
    @size = size
    @type = type
    @crust = crust
  end

  def new_order(args)
    size = args[:size]
    type = args[:type]
    crust = args[:crust]

  end

  def full_order
  end

end
